{
  "extends": "../../.eslintrc.json",
  "ignorePatterns": ["!**/*"],
  "overrides": [
    {
      "files": ["*.ts"],
      "parserOptions": {
        "project": ["projects/integration/tsconfig.(app|spec).json"]
      },
      "rules": {}
    },
    { "files": ["*.html"], "rules": {} }
  ]
}
