{
  "extends": "../../.eslintrc.json",
  "ignorePatterns": ["!**/*"],
  "overrides": [
    {
      "files": ["*.ts"],
      "parserOptions": {
        "project": ["projects/signal-store/tsconfig.(lib|spec).json"]
      },
      "rules": {}
    },
    { "files": ["*.html"], "rules": {} }
  ]
}
